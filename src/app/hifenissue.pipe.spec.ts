import { HifenissuePipe } from './hifenissue.pipe';

describe('HifenissuePipe', () => {
  it('create an instance', () => {
    const pipe = new HifenissuePipe();
    expect(pipe).toBeTruthy();
  });
});
